import 'package:flutter/material.dart';
import 'package:social_media_app/Modules/auth/sign_up/body.dart';

class SignUPScreen extends StatelessWidget {
  @override

  @override
  Widget build(BuildContext context) {
    return Scaffold(

        body: SafeArea(child: Body()));
  }
}
